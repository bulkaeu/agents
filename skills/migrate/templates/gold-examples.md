# Gold examples (optional, pointers only)

Prefer the profile's `## Gold examples` section. Use this file only when the pointer table needs more
explanation than a table row holds.

Clone these ports. **Do not paste their source into prompts** — point at paths.

| path | teaches |
| --- | --- |
